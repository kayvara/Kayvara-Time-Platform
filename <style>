        
        /* PALET WARNA DASAR & VARIASI TEMA KEKINIAN */
        :root {
            --bg-dark: #020204;
            --panel-glass: rgba(15, 5, 8, 0.65); 
            --neon-red: #ff1e27;      
            --neon-crimson: #9e0006;  
            --neon-purple: #7000ad;   
            --neon-orange: #ff5500;   
            --neon-gold: #ffaa00;     
            --neon-blue: #00a2ff;     
        }

        body.theme-akatsuki {
            --bg-dark: #020204;
            --panel-glass: rgba(15, 5, 8, 0.65);
            --neon-red: #ff1e27;
            --neon-crimson: #9e0006;
            --neon-purple: #7000ad;
        }

        body.theme-cyberpunk {
            --bg-dark: #090214;
            --panel-glass: rgba(22, 10, 35, 0.65);
            --neon-red: #00f0ff;      
            --neon-crimson: #ff0055;  
            --neon-purple: #9d00ff;
            --neon-orange: #00ffaa;
            --neon-gold: #ff00aa;
        }

        body.theme-hacker {
            --bg-dark: #010804;
            --panel-glass: rgba(5, 20, 10, 0.65);
            --neon-red: #39ff14;      
            --neon-crimson: #008f11;
            --neon-purple: #00ff88;
            --neon-orange: #adff2f;
            --neon-gold: #ffff00;
        }

        body.theme-gold {
            --bg-dark: #080601;
            --panel-glass: rgba(22, 18, 10, 0.65);
            --neon-red: #ffcc00;      
            --neon-crimson: #997700;
            --neon-purple: #ff6600;
            --neon-orange: #ffaa00;
            --neon-gold: #ffffff;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: var(--bg-dark);
            font-family: 'Plus Jakarta Sans', sans-serif;
            color: #ffffff;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow-x: hidden;
            perspective: 1000px;
            padding: 20px;
            padding-top: 80px; 
            transition: background-color 0.5s ease;
        }

        body.theme-akatsuki {
            background-image: 
                radial-gradient(circle at 10% 20%, rgba(255, 30, 39, 0.12) 0%, transparent 45%),
                radial-gradient(circle at 90% 80%, rgba(112, 0, 173, 0.12) 0%, transparent 45%),
                radial-gradient(circle at center, #0d0406 0%, #020204 100%);
        }
        body.theme-cyberpunk {
            background-image: 
                radial-gradient(circle at 10% 20%, rgba(0, 240, 255, 0.12) 0%, transparent 45%),
                radial-gradient(circle at 90% 80%, rgba(255, 0, 85, 0.12) 0%, transparent 45%),
                radial-gradient(circle at center, #110521 0%, #090214 100%);
        }
        body.theme-hacker {
            background-image: 
                radial-gradient(circle at 10% 20%, rgba(57, 255, 20, 0.12) 0%, transparent 45%),
                radial-gradient(circle at 90% 80%, rgba(0, 255, 136, 0.12) 0%, transparent 45%),
                radial-gradient(circle at center, #02140a 0%, #010804 100%);
        }
        body.theme-gold {
            background-image: 
                radial-gradient(circle at 10% 20%, rgba(255, 204, 0, 0.08) 0%, transparent 45%),
                radial-gradient(circle at 90% 80%, rgba(255, 102, 0, 0.08) 0%, transparent 45%),
                radial-gradient(circle at center, #1c1405 0%, #080601 100%);
        }

        #crowCanvas {
            position: fixed;
            top: 0; left: 0;
            width: 100vw; height: 100vh;
            z-index: 1;          
            pointer-events: none; 
        }

        .central-container {
            width: 100%;
            max-width: 1200px;
            margin: auto;
            padding: 20px;
            text-align: center;
            z-index: 10; 
            animation: popInContainer 0.8s cubic-bezier(0.16, 1, 0.3, 1);
        }

        /* LIVE CLOCK & CONTROLS */
        .mini-clock-box {
            font-family: 'Orbitron', sans-serif;
            font-size: 11px;
            letter-spacing: 2px;
            color: #7a5257; 
            text-transform: uppercase;
            font-weight: 700;
            margin-bottom: 20px;
            text-shadow: 0 0 8px rgba(255, 30, 39, 0.2);
            display: inline-flex;
            align-items: center;
            gap: 10px;
            background: rgba(15, 5, 8, 0.4);
            padding: 6px 16px;
            border-radius: 20px;
            border: 1px solid rgba(255, 30, 39, 0.05);
            position: relative;
        }

        .mini-clock-box #live-time {
            color: #ffffff;
            text-shadow: 0 0 10px var(--neon-red);
        }

        /* DROPDOWN CONTAINER TEMA */
        .theme-selector-container {
            position: relative;
            display: inline-flex;
            align-items: center;
        }

        .theme-btn {
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 4px;
            color: var(--neon-red);
            font-weight: 900;
            transition: all 0.3s ease;
        }

        .theme-btn:hover {
            filter: brightness(1.3);
            text-shadow: 0 0 8px var(--neon-red);
        }

        .theme-dropdown {
            position: absolute;
            top: calc(100% + 12px);
            right: 0;
            background: rgba(10, 5, 8, 0.95);
            border: 1px solid var(--neon-red);
            border-radius: 12px;
            padding: 6px 0;
            width: 180px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.8), 0 0 15px rgba(255,30,39,0.1);
            display: none;
            z-index: 1001;
            text-align: left;
            backdrop-filter: blur(10px);
        }

        .theme-dropdown.show {
            display: block;
            animation: dropdownFade 0.25s cubic-bezier(0.16, 1, 0.3, 1);
        }

        .theme-option {
            padding: 10px 14px;
            font-family: 'Orbitron', sans-serif;
            font-size: 11px;
            color: #a38588;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 10px;
            transition: all 0.2s ease;
        }

        .theme-option:hover {
            background: rgba(255, 255, 255, 0.06);
            color: #ffffff;
        }

        .theme-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
        }

        .dot-akatsuki { background: #ff1e27; box-shadow: 0 0 6px #ff1e27; }
        .dot-cyberpunk { background: #00f0ff; box-shadow: 0 0 6px #00f0ff; }
        .dot-hacker { background: #39ff14; box-shadow: 0 0 6px #39ff14; }
        .dot-gold { background: #ffcc00; box-shadow: 0 0 6px #ffcc00; }

        .portal-brand {
            font-family: 'Orbitron', sans-serif;
            font-size: calc(24px + 2vw);
            font-weight: 900;
            letter-spacing: 8px;
            background: linear-gradient(135deg, #ffffff 20%, var(--neon-red) 80%);
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: 0 0 25px rgba(255, 30, 39, 0.55);
            margin-bottom: 8px;
            text-transform: uppercase;
        }

        .portal-subtitle {
            font-family: 'Orbitron', sans-serif;
            font-size: calc(10px + 0.3vw);
            color: #7a5257; 
            letter-spacing: 4px;
            margin-bottom: 40px;
            text-transform: uppercase;
            font-weight: 700;
            text-shadow: 0 0 10px rgba(255, 30, 39, 0.2);
        }

        .menu-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 25px;
            margin-bottom: 40px;
            width: 100%;
        }

        .menu-card {
            background: var(--panel-glass);
            -webkit-backdrop-filter: blur(16px);
            backdrop-filter: blur(16px);
            border: 1px solid rgba(255, 30, 39, 0.08); 
            border-radius: 20px;
            padding: 40px 20px;
            cursor: pointer;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 20px;
            position: relative;
            overflow: hidden;
            transition: all 0.4s cubic-bezier(0.15, 0.85, 0.33, 1);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.65), inset 0 1px 1px rgba(255, 30, 39, 0.1);
            text-decoration: none;
            z-index: 1;
        }

        .menu-card::before {
            content: '';
            position: absolute;
            top: 0; left: -100%;
            width: 50%; height: 100%;
            background: linear-gradient(to right, transparent, rgba(255, 30, 39, 0.15), transparent);
            transform: skewX(-25deg);
            transition: 0.7s;
        }
        .menu-card:hover::before { left: 150%; }

        .menu-card::after {
            content: '';
            position: absolute;
            top: 50%; left: 50%;
            transform: translate(-50%, -50%) scale(0.6);
            width: 100%; height: 100%;
            background-image: url('gagak.png');
            background-size: contain;
            background-repeat: no-repeat;
            background-position: center;
            filter: brightness(0) drop-shadow(0 0 15px var(--neon-red)); 
            opacity: 0;
            z-index: -1;
            transition: all 0.5s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            pointer-events: none;
        }

        .menu-card i {
            font-size: 48px;
            color: #6e5457; 
            transition: all 0.4s ease;
        }

        .menu-card span {
            font-family: 'Orbitron', sans-serif;
            font-size: 14px;
            font-weight: 700;
            letter-spacing: 1px;
            color: #a38588;
            transition: all…
</style>
</head>
